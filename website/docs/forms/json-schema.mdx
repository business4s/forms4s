---
sidebar_position: 4
---
import RawMarkdown from '@site/src/components/RawMarkdown';
import SbtDependency from '@site/src/components/SbtDependency';

# JSON Schema Integration

forms4s provides integration with JSON Schema, allowing you to generate form elements directly from a JSON Schema definition.
This makes it easy to create forms based on existing data models.

## Usage

<SbtDependency moduleName={"forms4s-jsonschema"}/>

```scala file=./main/scala/forms4s/example/docs/JsonSchemaSupportExample.scala start=start_doc end=end_doc
```

## Limitations

- Not all JSON Schema validation keywords are supported yet
- Complex schema combinations (allOf, anyOf) are not fully supported
- Custom formats may require additional configuration

For more advanced use cases, you may need to create form elements manually or extend the `FormFromJsonSchema` module.

## Supported types

<RawMarkdown file="docs/json-schema-form-elements.md" />

## Supported validations

TODO :)
